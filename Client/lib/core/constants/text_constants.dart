/// [TextConstants] stores all text constants used across the app. Anything other
/// than text constants should not be in this class.
class TextConstants {
  const TextConstants._();

  // Text not having translations
  static const ok = 'OK';
}
